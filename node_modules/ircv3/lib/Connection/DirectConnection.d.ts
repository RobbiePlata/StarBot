import Connection from './Connection';
declare class DirectConnection extends Connection {
    private _socket?;
    readonly port: number;
    connect(): Promise<void>;
    disconnect(): void;
    sendRaw(line: string): void;
}
export default DirectConnection;

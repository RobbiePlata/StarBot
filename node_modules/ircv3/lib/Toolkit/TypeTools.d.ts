import Message from '../Message/Message';
export declare type MessageDataType<T> = T extends Message<infer D> ? D : never;
export declare type MessageParams<T> = {
    [name in keyof MessageDataType<T>]?: string;
};
export declare type ConstructedType<C> = C extends {
    new (...params: any[]): infer T;
} ? T : never;
